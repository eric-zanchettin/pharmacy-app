import styled from 'styled-components';

export const BubbleButton = styled.button`
    position: fixed;
    right: 1rem;
    bottom: 1rem;
    
    border-radius: 200px;
    background: #424ef5;
    padding: 0.8rem 1.2rem;

    p {
        margin-top: 4px;
        color: #FFF;
        font-size: 2rem;
    }
`;